---

const { url, title, description } = Astro.props;
---

<a href={url} class="transition ease-in-out p-2 rounded-lg hover:bg-[#597e8d]">
    <h3 class="text-[2em] font-semibold">{ title }</h3>
    <p class="text-xl text-wrap">{ description }</p>
</a>