---

const { url, title, description } = Astro.props;
---

<a href={url} class="transition ease-in-out p-2 rounded-lg hover:bg-slate-700">
    <h3 class="text-[2em] font-semibold underline underline-offset-8">{ title }</h3>
    <p class="text-xl text-wrap mt-2">{ description }</p>
</a>